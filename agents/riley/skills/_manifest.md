# Riley — Skills Manifest

## Available Skills (On Disk)
- None detected in `agents/riley/skills` (only `skills-lock.json` exists).

## Active Skills for Loader
- None

## Notes
- `skills-lock.json` references `project-manager` and `solana-dev`, but those skill files are not present on disk.
